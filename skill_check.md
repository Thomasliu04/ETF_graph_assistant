# AI审核校验专用Skill
skill_name: etf_report_review
skill_version: 0.3.0
data_contract_version: 0.3.0
# 与 scripts/contracts/ 及 skills.md 的 data_contract_version 保持一致

## 输入内容
1. 多张原始数据表（区域、赛道、管理人、国家队、目标公司总览/分区域/分赛道/产品梯队）
2. AI生成的完整ETF分析报告文本（或增量复审时的变更章节）

## 核心校验规则（不可逾越）

## 一、整体可信度得分（0-100）
扣分规则：
1. 数据造假/数值错误：单次扣20分
2. 逻辑矛盾：单次扣10分
3. 关键内容遗漏：扣15分
4. 轻微表述不严谨：单次扣3分
5. 关键数字缺少来源标注：单次扣5分
6. 将全市场赛道TOP产品误写为目标公司产品：单次扣20分
总分≥80：仅少量瑕疵，人工快速浏览即可
60≤总分<80：存在多处错误，需要按章节局部改写
总分<60：严重失真，需要带反馈全文重写

## 二、错误明细清单（每条附带证据）
1. 错误类型：数据失真
   报告原文：xxx
   原始表格真实数据：xxx
   所属章节：area|track|manager|target
   修正建议：xxx
2. 错误类型：业务逻辑矛盾
   报告原文：xxx
   公式/业务规则依据：xxx
   所属章节：area|track|manager|target
3. 错误类型：内容缺失
   缺失模块：目标公司分赛道/产品梯队/横向对标
   所属章节：area|track|manager|target
4. 错误类型：证据缺失
   报告原文：xxx
   缺失内容：未标注来源表/筛选条件/字段名
   所属章节：area|track|manager|target
   修正建议：补充类似“来源：target_summary.规模增量”的证据来源

## 三、必须核对的目标公司要点
1. 整体期初/期末/增量/增速是否与 target_summary 一致
2. 分区域、分赛道结论是否能在 target_by_area / target_by_track 找到对应行
3. 增量TOP与缩水TOP是否来自 target_top_bottom，而非 track_agg 的全市场TOP产品
4. 横向对标中的排名/增量是否来自 manager_agg

## 四、合规结论
1. 是否存在虚构数据：是/否
2. 是否完整覆盖4大分析维度：是/否
3. 是否建议人工深度复核：是（低分/大量错误）/否（仅瑕疵）
4. 是否需要重新调用生成AI改写报告：是/否
5. 关键数字是否均带来源标注：是/否

## 输出固定格式
先输出人类可读的审核说明（含错误明细与合规结论），正文中必须包含一行：`总分：xx`（0-100整数）。

然后在全文末尾追加且仅追加一个 JSON 代码块，供程序解析（字段名固定）：

```json
{
  "score": 72,
  "errors": [
    {
      "type": "数据失真",
      "section": "target",
      "quote": "报告中的错误原文",
      "evidence": "对照表中的真实数据或规则",
      "fix": "具体修正建议"
    }
  ],
  "failed_sections": ["target", "manager"],
  "rewrite_instructions": "给生成侧的压缩重写指令"
}
```

字段约束：
- `score`：0-100 整数，须与正文 `总分：xx` 一致
- `section` / `failed_sections` 仅允许：`area` / `track` / `manager` / `target`
- `failed_sections`：所有需要改写的章节键；无错误时为空数组
- `errors`：每条必须带 `section`；无错误时为空数组
- `rewrite_instructions`：简洁可执行的改写要点

禁止主观评价，所有判定必须有客观数据/公式依据。

## 增量复审补充规则
若输入标明为增量复审：
1. 重点核对「已改写章节」是否已消除上一轮错误
2. 检查改写是否引入新的数据失真或逻辑矛盾
3. 未改写章节默认沿用上一轮结论，除非全文出现明显跨章节矛盾
4. 仍须给出全文可信度总分，并输出同样结构的 JSON 代码块

## 限制条件
仅允许使用传入表格内数据，不得依赖外部行业常识推断；无数据支撑的观点一律判定失真。
